import { Routes } from '@angular/router';
import { RoadmapListComponent } from './roadmap-list.component';

export const RoadmapListRoutes: Routes = [
    {
        path: '',
        component: RoadmapListComponent
    }
];
